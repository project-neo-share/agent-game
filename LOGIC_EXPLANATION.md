# 🧠 윤리 엔진 핵심 로직 상세 설명

## 📚 목차
1. [FRAMEWORKS란 무엇인가?](#1-frameworks란-무엇인가)
2. [가중 투표 의사결정 로직](#2-가중-투표-의사결정-로직)
3. [자율 판단 의사결정 로직](#3-자율-판단-의사결정-로직)
4. [메트릭 계산 로직](#4-메트릭-계산-로직)

---

## 1. FRAMEWORKS란 무엇인가?

### 🎯 정의
```python
FRAMEWORKS = ["emotion", "social", "moral", "identity"]
```

FRAMEWORKS는 **4가지 윤리적 가치 체계**를 의미합니다.

### 📖 각 프레임워크 상세 설명

#### 1️⃣ **Emotion (감정)**
- **의미**: 감정적 반응, 공감, 직관적 판단
- **특징**: 
  - "마음이 시키는 대로" 행동
  - 즉각적인 감정 반응 중시
  - 결과의 감정적 영향 고려
- **예시**:
  - "5명을 구하고 싶다" (강한 감정)
  - "한 명이라도 죽게 만들고 싶지 않다" (공포, 죄책감)
- **윤리 이론**: 감정주의 윤리학 (Emotivism)

#### 2️⃣ **Social (사회적 관계)**
- **의미**: 사회적 규범, 타인과의 관계, 평판, 협력
- **특징**:
  - 사회적 책임감 중시
  - 타인의 시선과 평가 고려
  - 집단의 이익과 조화
- **예시**:
  - "사회가 나를 어떻게 볼까?"
  - "이 선택이 공동체에 미치는 영향은?"
  - "관계가 깨지지 않을까?"
- **윤리 이론**: 사회계약론 (Social Contract Theory)

#### 3️⃣ **Moral (도덕적 규범)**
- **의미**: 도덕적 원칙, 규칙, 금기, 의무
- **특징**:
  - 절대적 도덕 법칙 존재
  - "해서는 안 되는 것"이 있음
  - 원칙과 규칙 준수 중시
- **예시**:
  - "사람을 직접 죽이면 안 된다" (살인 금기)
  - "규칙을 어기면 안 된다"
  - "거짓말은 나쁘다"
- **윤리 이론**: 의무론 (Deontology, 칸트 윤리학)

#### 4️⃣ **Identity (정체성)**
- **의미**: 자아 정체성, 장기적 가치관, 일관성
- **특징**:
  - "나는 어떤 사람인가?"
  - 자신의 가치관과 일치하는 선택
  - 장기적 관점에서 후회하지 않을 선택
- **예시**:
  - "나는 결단력 있는 사람이다"
  - "나는 원칙을 지키는 사람이다"
  - "이 선택이 나를 정의한다"
- **윤리 이론**: 미덕 윤리학 (Virtue Ethics)

---

### 🎭 시나리오별 프레임워크 투표 예시

#### S1 (고전적 트롤리)
```python
votes = {
    "emotion": "A",    # 5명 구하기 → 감정적으로 옳음
    "social": "B",     # 개입하지 않기 → 사회적 책임 회피
    "moral": "B",      # 직접 죽이지 않기 → 도덕적 금기 준수
    "identity": "A"    # 결단력 → 적극적 자아
}
```

**해석**:
- Emotion: "5명이나 구할 수 있다면 A를 선택해야지!"
- Social: "내가 직접 개입하면 책임을 져야 해. B가 낫겠어."
- Moral: "사람을 직접 죽이는 건 절대 안 돼. B!"
- Identity: "난 위기에 행동하는 사람이야. A!"

---

## 2. 가중 투표 의사결정 로직

### 🗳️ 함수 원형
```python
def majority_vote_decision(scn: Scenario, weights: Dict[str, float]) -> Tuple[str, Dict[str, float]]:
    """
    가중 투표 방식으로 의사결정
    
    Parameters:
        scn: 시나리오 객체
        weights: 각 프레임워크의 가중치 {"emotion": 0.35, "social": 0.25, ...}
    
    Returns:
        decision: "A" 또는 "B"
        align: {"A": 점수, "B": 점수}
    """
```

### 📊 작동 원리

#### 1단계: 각 프레임워크의 투표 가져오기
```python
scn.votes = {
    "emotion": "A",
    "social": "B",
    "moral": "B",
    "identity": "A"
}
```

#### 2단계: A를 선택한 프레임워크들의 가중치 합산
```python
# A를 선택한 프레임워크: emotion, identity
a = sum(weights[f] for f in FRAMEWORKS if scn.votes[f] == "A")

# 구체적 계산:
a = weights["emotion"] + weights["identity"]
a = 0.35 + 0.20
a = 0.55
```

#### 3단계: B를 선택한 프레임워크들의 가중치 합산
```python
# B를 선택한 프레임워크: social, moral
b = sum(weights[f] for f in FRAMEWORKS if scn.votes[f] == "B")

# 구체적 계산:
b = weights["social"] + weights["moral"]
b = 0.25 + 0.20
b = 0.45
```

#### 4단계: 더 높은 점수를 받은 쪽 선택
```python
decision = "A" if a >= b else "B"
# 0.55 >= 0.45 → True
# decision = "A"
```

### 🎯 실전 예시

#### 예시 1: 공리주의자 (Emotion 100%)
```python
weights = {
    "emotion": 1.0,
    "social": 0.0,
    "moral": 0.0,
    "identity": 0.0
}

# S1 시나리오
a = 1.0  # emotion만 카운트
b = 0.0  # 아무것도 카운트 안됨
결과: "A" 선택 (5명 구하기)
```

#### 예시 2: 의무론자 (Moral 100%)
```python
weights = {
    "emotion": 0.0,
    "social": 0.0,
    "moral": 1.0,
    "identity": 0.0
}

# S1 시나리오
a = 0.0
b = 1.0  # moral만 카운트
결과: "B" 선택 (직접 죽이지 않기)
```

#### 예시 3: 균형잡힌 사람 (기본값)
```python
weights = {
    "emotion": 0.35,
    "social": 0.25,
    "moral": 0.20,
    "identity": 0.20
}

# S1 시나리오
a = 0.35 + 0.20 = 0.55
b = 0.25 + 0.20 = 0.45
결과: "A" 선택 (감정과 정체성이 약간 우세)
```

### 💡 핵심 포인트

1. **가중치 정규화**: 합이 1이 되도록 자동 조정
   ```python
   원래: {emotion: 2, social: 1, moral: 1, identity: 1}
   정규화: {emotion: 0.4, social: 0.2, moral: 0.2, identity: 0.2}
   ```

2. **동점 처리**: a >= b이므로 동점이면 A 선택

3. **사용자 맞춤**: 슬라이더로 가중치 조정 → 사용자의 가치관 반영

---

## 3. 자율 판단 의사결정 로직

### 🤖 함수 원형
```python
def autonomous_decision(scn: Scenario, prev_trust: float) -> str:
    """
    자율 판단: 데이터 기반으로 AI가 스스로 결정
    
    Parameters:
        scn: 시나리오 객체
        prev_trust: 이전 라운드의 신뢰도 (0~1)
    
    Returns:
        decision: "A" 또는 "B"
    """
```

### 📊 작동 원리

#### 1단계: 메타데이터 가져오기
```python
metaA = scn.base["A"]  # A 선택의 메트릭
metaB = scn.base["B"]  # B 선택의 메트릭

# 예시 (S1):
metaA = {
    "lives_saved": 5,
    "lives_harmed": 1,
    "fairness_gap": 0.35,
    "rule_violation": 0.60,
    "regret_risk": 0.40
}

metaB = {
    "lives_saved": 0,
    "lives_harmed": 5,
    "fairness_gap": 0.50,
    "rule_violation": 0.20,
    "regret_risk": 0.60
}
```

#### 2단계: 점수 계산 함수 정의
```python
def score(meta, accept_base):
    """
    선택지의 종합 점수 계산
    
    5가지 요소 고려:
    1. 사회적 수용도 (40%)
    2. 공리주의적 효용 (25%)
    3. 공정성 (20%)
    4. 규칙 준수 (10%)
    5. 후회 위험 (5%)
    """
```

##### 2-1. 공리주의적 효용 계산
```python
harm = meta["lives_harmed"]    # 피해 생명
save = meta["lives_saved"]     # 구한 생명

util = (save - harm) / max(1.0, save + harm)

# A의 경우:
util_A = (5 - 1) / (5 + 1) = 4/6 = 0.667
# 해석: 순효용이 높음 (더 많은 생명 구함)

# B의 경우:
util_B = (0 - 5) / (0 + 5) = -5/5 = -1.0
# 해석: 순효용이 매우 낮음 (많은 피해)
```

##### 2-2. 공정성 점수
```python
fair = 1 - meta["fairness_gap"]

# A의 경우:
fair_A = 1 - 0.35 = 0.65
# 해석: 다소 공정함

# B의 경우:
fair_B = 1 - 0.50 = 0.50
# 해석: 보통 수준
```

##### 2-3. 규칙 준수 점수
```python
rule = 1 - meta["rule_violation"]

# A의 경우:
rule_A = 1 - 0.60 = 0.40
# 해석: 규칙 위반이 큼

# B의 경우:
rule_B = 1 - 0.20 = 0.80
# 해석: 규칙을 잘 지킴
```

##### 2-4. 후회 위험 점수
```python
regret = 1 - meta["regret_risk"]

# A의 경우:
regret_A = 1 - 0.40 = 0.60
# 해석: 후회 위험 낮음

# B의 경우:
regret_B = 1 - 0.60 = 0.40
# 해석: 후회 위험 높음
```

##### 2-5. 가중 합산
```python
score = (
    0.40 * accept_base +    # 40% - 사회적 수용도
    0.25 * util +           # 25% - 공리주의적 효용
    0.20 * fair +           # 20% - 공정성
    0.10 * rule +           # 10% - 규칙 준수
    0.05 * regret           #  5% - 후회 위험
)
```

#### 3단계: 기본 수용도 설정
```python
a_base = scn.accept["A"]  # 0.70 (S1 기준)
b_base = scn.accept["B"]  # 0.50 (S1 기준)

# 특별 케이스: S4에서 A 선택 시 페널티
if scn.sid == "S4" and choice == "A":
    a_base -= 0.15
    # 이유: 개발자 희생은 사회적으로 덜 수용됨
```

#### 4단계: 특별 케이스 - S5 (이전 신뢰도 반영)
```python
if scn.sid == "S5":
    # 신뢰도가 낮으면 A(규제 강화) 선호
    a_base = clamp(a_base + 0.25*(1 - prev_trust), 0, 1)
    
    # 신뢰도가 높으면 B(자율성) 선호
    b_base = clamp(b_base + 0.25*(prev_trust), 0, 1)

# 예시:
# prev_trust = 0.3 (낮음)
# a_base = 0.55 + 0.25*(1-0.3) = 0.55 + 0.175 = 0.725
# b_base = 0.55 + 0.25*(0.3) = 0.55 + 0.075 = 0.625
# → A 선택 (규제 강화)
```

#### 5단계: 최종 점수 계산 및 선택
```python
scoreA = score(metaA, a_base)
scoreB = score(metaB, b_base)

# S1 예시 계산:
scoreA = (
    0.40 * 0.70 +      # 0.280
    0.25 * 0.667 +     # 0.167
    0.20 * 0.65 +      # 0.130
    0.10 * 0.40 +      # 0.040
    0.05 * 0.60        # 0.030
) = 0.647

scoreB = (
    0.40 * 0.50 +      # 0.200
    0.25 * (-1.0) +    # -0.250
    0.20 * 0.50 +      # 0.100
    0.10 * 0.80 +      # 0.080
    0.05 * 0.40        # 0.020
) = 0.150

# 결정
return "A" if scoreA >= scoreB else "B"
# 0.647 >= 0.150 → "A" 선택
```

### 💡 핵심 포인트

1. **데이터 기반**: 사용자 가중치 대신 시나리오 메타데이터 활용
2. **복합 평가**: 5가지 다른 관점을 통합
3. **적응형**: 이전 신뢰도를 반영 (S5)
4. **페널티 시스템**: 특정 선택에 불이익 (S4)

---

## 4. 메트릭 계산 로직

### 📈 함수 원형
```python
def compute_metrics(
    scn: Scenario,           # 시나리오
    choice: str,             # 선택 ("A" or "B")
    weights: Dict[str, float],  # 가중치
    align: Dict[str, float], # 정렬 점수
    prev_trust: float        # 이전 신뢰도
) -> Dict[str, Any]:
    """
    선택에 대한 11가지 메트릭 계산
    """
```

### 📊 계산되는 11가지 메트릭

#### 1️⃣ **기본 메트릭** (시나리오에서 가져옴)
```python
m = dict(scn.base[choice])

# 예시 (S1, A 선택):
m = {
    "lives_saved": 5,        # 구한 생명
    "lives_harmed": 1,       # 잃은 생명
    "fairness_gap": 0.35,    # 불공정 정도
    "rule_violation": 0.60,  # 규칙 위반 정도
    "regret_risk": 0.40      # 후회 위험
}
```

#### 2️⃣ **수용도 조정** (특별 케이스 처리)
```python
accept_base = scn.accept[choice]  # 기본 수용도

# S4에서 A 선택 시 페널티
if scn.sid == "S4" and choice == "A":
    accept_base -= 0.15

# S5에서 이전 신뢰도 반영
if scn.sid == "S5":
    if choice == "B":
        accept_base += 0.25 * prev_trust
    else:
        accept_base += 0.25 * (1 - prev_trust)

# 0~1 범위로 제한
accept_base = clamp(accept_base, 0, 1)
```

#### 3️⃣ **공리주의적 효용** (Utilitarian Utility)
```python
util = (m["lives_saved"] - m["lives_harmed"]) / max(1.0, m["lives_saved"] + m["lives_harmed"])

# S1, A 선택:
util = (5 - 1) / (5 + 1) = 4/6 = 0.667

# 해석:
# +1.0: 완벽한 효용 (모두 구함, 아무도 안죽음)
#  0.0: 중립
# -1.0: 최악의 효용 (모두 죽음)
```

#### 4️⃣ **시민 감정** (Citizen Sentiment)
```python
citizen_sentiment = clamp(
    accept_base - 
    0.35 * m["rule_violation"] -    # 규칙 위반은 감정을 나쁘게 함
    0.20 * m["fairness_gap"] +      # 불공정도 나쁘게 함
    0.15 * util,                    # 좋은 결과는 감정을 좋게 함
    0, 1
)

# S1, A 선택:
citizen_sentiment = clamp(
    0.70 - 0.35*0.60 - 0.20*0.35 + 0.15*0.667,
    0, 1
)
= clamp(0.70 - 0.21 - 0.07 + 0.10, 0, 1)
= clamp(0.52, 0, 1)
= 0.52 (보통 수준의 긍정적 감정)
```

#### 5️⃣ **규제 압력** (Regulation Pressure)
```python
regulation_pressure = clamp(
    1 - citizen_sentiment +         # 감정이 나쁘면 규제 압력 증가
    0.20 * m["regret_risk"],        # 후회 위험도 압력 증가
    0, 1
)

# S1, A 선택:
regulation_pressure = clamp(
    1 - 0.52 + 0.20*0.40,
    0, 1
)
= clamp(0.48 + 0.08, 0, 1)
= 0.56 (중간 수준의 규제 압력)
```

#### 6️⃣ **이해관계자 만족도** (Stakeholder Satisfaction)
```python
stakeholder_satisfaction = clamp(
    0.5 * (1 - m["fairness_gap"]) +    # 50% - 공정성
    0.3 * util +                        # 30% - 효용
    0.2 * (1 - m["rule_violation"]),   # 20% - 규칙 준수
    0, 1
)

# S1, A 선택:
stakeholder_satisfaction = clamp(
    0.5*(1-0.35) + 0.3*0.667 + 0.2*(1-0.60),
    0, 1
)
= clamp(0.325 + 0.200 + 0.080, 0, 1)
= 0.605 (중상 수준의 만족도)
```

#### 7️⃣ **윤리적 일관성** (Ethical Consistency)
```python
consistency = clamp(align[choice], 0, 1)

# S1, A 선택, 가중 투표에서:
# align = {"A": 0.55, "B": 0.45}
consistency = 0.55

# 해석:
# 사용자가 설정한 가중치와 얼마나 일치하는가?
# 높을수록 = 사용자의 가치관과 일치
```

#### 8️⃣ **사회적 신뢰** (Social Trust)
```python
trust = clamp(
    0.5 * citizen_sentiment +           # 50% - 시민 감정
    0.25 * (1 - regulation_pressure) +  # 25% - (규제 압력 낮을수록 좋음)
    0.25 * stakeholder_satisfaction,    # 25% - 만족도
    0, 1
)

# S1, A 선택:
trust = clamp(
    0.5*0.52 + 0.25*(1-0.56) + 0.25*0.605,
    0, 1
)
= clamp(0.26 + 0.11 + 0.151, 0, 1)
= 0.521 (중간 수준의 신뢰)
```

#### 9️⃣ **AI 신뢰 점수** (AI Trust Score) ⭐ 최종 점수
```python
ai_trust_score = 100.0 * math.sqrt(consistency * trust)

# S1, A 선택:
ai_trust_score = 100.0 * math.sqrt(0.55 * 0.521)
= 100.0 * math.sqrt(0.287)
= 100.0 * 0.536
= 53.6점

# 해석:
# 100점: 완벽 (일관성 + 신뢰 모두 높음)
# 50점: 보통
# 0점: 최악
```

### 🎯 AI 신뢰 점수의 의미

#### 왜 기하평균인가?
```python
# 산술평균이라면:
score_arithmetic = (consistency + trust) / 2
= (0.55 + 0.521) / 2 = 0.536 (53.6점)

# 기하평균:
score_geometric = sqrt(consistency * trust)
= sqrt(0.55 * 0.521) = 0.536 (53.6점)
```

**이 경우는 우연히 비슷하지만, 차이는 극단적인 경우에 나타남:**

```python
# 케이스 1: 둘 다 보통
consistency = 0.5, trust = 0.5
산술: (0.5+0.5)/2 = 0.5 = 50점
기하: sqrt(0.5*0.5) = 0.5 = 50점

# 케이스 2: 한쪽이 극단적으로 낮음
consistency = 1.0, trust = 0.1
산술: (1.0+0.1)/2 = 0.55 = 55점  ← 여전히 괜찮아 보임
기하: sqrt(1.0*0.1) = 0.316 = 31.6점  ← 낮은 쪽에 더 많이 영향받음

# 케이스 3: 한쪽이 0
consistency = 0.8, trust = 0.0
산술: (0.8+0.0)/2 = 0.4 = 40점
기하: sqrt(0.8*0.0) = 0.0 = 0점  ← 하나라도 0이면 전체가 0
```

**기하평균을 쓰는 이유:**
1. **균형 강제**: 일관성과 신뢰 둘 다 높아야 높은 점수
2. **치명적 결함 반영**: 하나라도 매우 낮으면 전체 점수 급락
3. **곱셈적 관계**: 일관성과 신뢰는 독립적이 아니라 상호작용

---

### 📊 전체 메트릭 흐름도

```
┌─────────────────────────────────────────────┐
│  1. 시나리오 기본 메트릭                      │
│     lives_saved, lives_harmed,               │
│     fairness_gap, rule_violation,            │
│     regret_risk                              │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  2. 파생 계산                                 │
│     util ← lives_saved, lives_harmed         │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  3. 사회적 메트릭                             │
│     citizen_sentiment ← accept_base, util    │
│     regulation_pressure ← sentiment          │
│     stakeholder_satisfaction ← 복합          │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  4. 신뢰 메트릭                               │
│     consistency ← 가중치 정렬                 │
│     social_trust ← 사회적 메트릭들            │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│  5. 최종 점수                                 │
│     ai_trust_score = 100 * √(cons × trust)   │
└─────────────────────────────────────────────┘
```

---

## 🎓 학생들을 위한 요약

### 핵심 개념 3가지

#### 1. **FRAMEWORKS = 4가지 윤리관**
```
Emotion  → "느낌상 이게 맞아"
Social   → "사회적으로 이게 맞아"
Moral    → "도덕적으로 이게 맞아"
Identity → "내 가치관상 이게 맞아"
```

#### 2. **가중 투표 = 민주적 의사결정**
```
각 프레임워크가 투표 → 가중치 곱하기 → 합산 → 더 높은 쪽 선택
```

#### 3. **자율 판단 = 데이터 기반 의사결정**
```
메타데이터 분석 → 5가지 기준 평가 → 점수 계산 → 더 높은 쪽 선택
```

---

## 💡 실험 아이디어

### 실험 1: 프레임워크 우선순위 변경
```
1. Emotion 100% → 어떤 결과?
2. Moral 100% → 어떤 결과?
3. 차이 비교
```

### 실험 2: 가중 투표 vs 자율 판단
```
1. 같은 가중치로 두 방법 실행
2. 결과 다른가?
3. 왜 다른가? (투표 vs 데이터)
```

### 실험 3: 메트릭 상관관계
```
1. A 선택 → AI 신뢰 점수 50점
2. B 선택 → AI 신뢰 점수 30점
3. 왜 차이나는가? 각 메트릭 비교
```

---

## 📚 더 공부하기

### 추천 자료
1. **의무론 vs 공리주의**: 칸트 vs 벤담
2. **트롤리 문제**: 철학적 딜레마의 고전
3. **윤리적 AI**: AI 의사결정의 투명성

### 코드 분석 순서
1. `FRAMEWORKS` 정의 이해
2. 각 시나리오의 `votes` 필드 분석
3. `majority_vote_decision()` 단계별 실행
4. `autonomous_decision()` 점수 계산 추적
5. `compute_metrics()` 메트릭 흐름 따라가기

